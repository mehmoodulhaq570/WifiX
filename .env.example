# WifiX Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# SECRET_KEY: Used for session encryption and security tokens
# IMPORTANT: Change this to a long, random string in production!
# Generate with: python -c "import os; print(os.urandom(24).hex())"
SECRET_KEY=change-this-to-a-very-long-random-secret-key-in-production

# REQUIRE_PIN: Enable PIN authentication for downloads
# 0 = disabled (no PIN required)
# 1 = enabled (PIN required for all downloads)
REQUIRE_PIN=0

# GLOBAL_PIN: Global PIN code for all files (only used if REQUIRE_PIN=1)
# Leave empty to disable global PIN
# Example: 1234
GLOBAL_PIN=

# =============================================================================
# FILE MANAGEMENT
# =============================================================================

# FILE_TTL_SECONDS: Automatic file deletion after N seconds
# 0 = keep files forever until manually deleted
# 3600 = 1 hour
# 86400 = 24 hours
# 604800 = 7 days
FILE_TTL_SECONDS=0

# CLEANUP_INTERVAL_SECONDS: How often to check for expired files
# Default: 60 seconds
# Only matters if FILE_TTL_SECONDS > 0
CLEANUP_INTERVAL_SECONDS=60

# =============================================================================
# NETWORK DISCOVERY
# =============================================================================

# ENABLE_ZEROCONF: Enable Zeroconf/mDNS/Bonjour service discovery
# 1 = enabled (recommended for LAN deployment)
# 0 = disabled (use if you don't want auto-discovery)
# 
# When enabled, WifiX broadcasts itself on the local network:
# Service: WifiX on <IP>._wifi-share._tcp.local.
# Clients can auto-discover the server without manual IP entry
ENABLE_ZEROCONF=1

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# CORS_ORIGINS: Comma-separated list of allowed origins for cross-origin requests
# Development example: http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173
# Production example: https://wifux.yourdomain.com
# Use * to allow all origins (NOT recommended for production)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173

# =============================================================================
# RATE LIMITING (Advanced)
# =============================================================================

# RATELIMIT_STORAGE_URL: Storage backend for rate limiting
# memory:// = in-memory (default, simple but not persistent)
# redis://localhost:6379 = Redis (recommended for production)
# Uncomment to use Redis:
# RATELIMIT_STORAGE_URL=redis://localhost:6379

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# PORT: Port number to run the server on
# Default: 5000
# PORT=5000

# HOST: Host address to bind to
# Default: 0.0.0.0 (all interfaces)
# HOST=0.0.0.0

# =============================================================================
# LOGGING (Advanced)
# =============================================================================

# LOG_LEVEL: Logging verbosity
# DEBUG = detailed debugging information
# INFO = general information (default)
# WARNING = warning messages only
# ERROR = error messages only
# LOG_LEVEL=INFO

# =============================================================================
# DEPLOYMENT EXAMPLES
# =============================================================================

# --- Example 1: Development ---
# SECRET_KEY=dev-secret-key-not-for-production
# REQUIRE_PIN=0
# FILE_TTL_SECONDS=0
# ENABLE_ZEROCONF=1
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# --- Example 2: Production LAN (No HTTPS) ---
# SECRET_KEY=Zx9mK2nP4qR8sT3vW7yB1cD6fG0hJ5lN
# REQUIRE_PIN=1
# GLOBAL_PIN=1234
# FILE_TTL_SECONDS=86400
# ENABLE_ZEROCONF=1
# CORS_ORIGINS=http://10.0.0.100:5000

# --- Example 3: Production Internet (HTTPS) ---
# SECRET_KEY=Aa2Bb3Cc4Dd5Ee6Ff7Gg8Hh9Ii0Jj1Kk
# REQUIRE_PIN=1
# GLOBAL_PIN=secure-pin-123
# FILE_TTL_SECONDS=3600
# ENABLE_ZEROCONF=0
# CORS_ORIGINS=https://wifux.yourdomain.com

# --- Example 4: Docker Deployment ---
# Use docker-compose.yml environment section or:
# docker run --env-file .env -p 5000:5000 wifux-app

# =============================================================================
# NOTES
# =============================================================================

# 1. Never commit .env file to version control
# 2. Keep .env.example in version control as a template
# 3. Generate strong SECRET_KEY for production
# 4. Enable PIN protection for public-facing deployments
# 5. Use HTTPS in production (see DEPLOYMENT.md)
# 6. Restrict CORS_ORIGINS in production
# 7. Consider Redis for rate limiting in high-traffic scenarios
# 8. Monitor disk space if FILE_TTL_SECONDS=0 (no auto-cleanup)
